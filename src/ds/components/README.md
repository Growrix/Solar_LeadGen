# DS components

Component variants are organized by platform:

- `shared/` — default, cross-platform components (current baseline).
- `mobile/` — app-like mobile variants.
- `desktop/` — desktop-specific variants.

Public imports remain via `@/ds` (`src/ds/index.ts`).
