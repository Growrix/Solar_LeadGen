# DS Mobile (app-like) components

This folder holds **mobile/app-like** DS component variants.

- Prefer keeping behavior (hooks/services) in shared layers.
- Keep styling DS-owned (classes/tokens) and use platform knobs (`data-platform="mobile"`) where appropriate.
