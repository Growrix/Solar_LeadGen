/**
 * DS Themes (Blueprint boundary)
 *
 * Theme values are currently defined via CSS variables in `src/app/globals.css`.
 * This module exists as the stable import surface for future theme objects/helpers.
 */

export {};
