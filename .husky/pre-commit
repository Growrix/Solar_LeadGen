# Prototype-first workflow: allow Tailwind utilities.
# (The old className validator was designed for the previous semantic-DS-only rule.)
echo "pre-commit: skipping className validator (prototype Tailwind is allowed)"
